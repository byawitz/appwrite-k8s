apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: appwrite
  name: appwrite-builds
  labels:
    app: appwrite
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
